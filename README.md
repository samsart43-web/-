LoveSync — Smart Love Calculator

Quick start


1. Frontend only (Netlify deploy)

cd frontend
npm install
npm run dev

Overview

This is a frontend-only project (no backend). The compatibility algorithm runs locally in the browser and is deterministic (same names → same result). The UI uses Vite + React + Tailwind CSS + Framer Motion. It's ready to deploy on Netlify.
